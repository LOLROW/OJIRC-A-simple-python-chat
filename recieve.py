from OJIRC import *

recieve("room_name", "client2's ipv4")

# Important: If connecting between networks you MUST use your ipv4 instead of client2's

# If your connecting over LAN then use client2's ipv4